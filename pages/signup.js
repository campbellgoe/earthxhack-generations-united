import styled from 'styled-components'

const SignUpPage = styled(() => {
  //jsx goes here (see JSX syntax)
  return (<>
    <h3>Green Youth Ambassador</h3>
  </>)
})`
 /* styles go here (see styled-components or SASS syntax) */
`

export default SignUpPage